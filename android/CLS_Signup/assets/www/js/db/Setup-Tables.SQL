CREATE TABLE IF NOT EXISTS SUBSCRIBERS (EMAIL_ADDRESS VARCHAR(255) PRIMARY KEY NOT NULL, FIRST_NAME VARCHAR(255), LAST_NAME VARCHAR(255), PREFERRED_FORMAT VARCHAR(255));

CREATE TABLE IF NOT EXISTS MC_OPTIONS (API_KEY VARCHAR(255) PRIMARY KEY NOT NULL, LIST_ID VARCHAR(255) NOT NULL);